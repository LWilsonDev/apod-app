export enum AppColors {
  dark = "#111111",
  semiDark = "#333333",
  light = "#FFF",
}

export enum spacing {
  small = 5,
  regular = 10,
  double = 20,
}

export const BUTTON_SIZE = 60;
export const TOP_MARGIN = 20;
